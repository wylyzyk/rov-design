import { Canvas, Controls, Meta, Story, Primary, Description } from "@storybook/blocks";
import * as ButtonStories from "./Button.stories";
import { Button } from "../button.tsx";

[//]: # (<Meta title="Component/General/Button &#40;按钮&#41;" component={Button} />)
<Meta of={ButtonStories} component={Button} />

export const Template = (args) => <Button {...args} />;


# Button 按钮

<Description />

## 何时使用

标记了一个（或封装一组）操作命令，响应用户点击行为，触发相应的业务逻辑。

在 Ant Design 中我们提供了五种按钮。

- 主按钮：用于主行动点，一个操作区域只能有一个主按钮。
- 默认按钮：用于没有主次之分的一组行动点。
- 文本按钮：用于最次级的行动点。
- 链接按钮：一般用于链接，即导航至某位置。

以及四种状态属性与上面配合使用。

- 危险：删除/移动/修改权限等危险操作，一般需要二次确认。
- 禁用：行动点不可用的时候，一般需要文案解释。

## 代码演示

> 点击右下角 *show code* 按钮, 可以查看源码
<Canvas>
    <Story of={ButtonStories.Primary} />
</Canvas>

<Canvas className={"code-wrap"}>
    <Story of={ButtonStories.BasicButton} />
</Canvas>

## API

通过设置 Button 的属性来产生不同的按钮样式

可以通过`Control`列的数值进行在线预览, 也可以点击左侧查看单独按钮组件

<Controls />

